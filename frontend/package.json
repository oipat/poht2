{
  "name": "blogi-flux",
  "version": "0.0.1",
  "description": "...",
  "main": "js/app.js",
  "dependencies": {
    "alt": "^0.15.5",
    "babelify": "^6.0.2",
    "react": "^0.13.1",
    "then-request": "^2.1.1"
  },
  "devDependencies": {
    "watchify": "^2.3.0"
  },
  "scripts": {
    "start": "watchify --debug -o build/bundle.js -v -d js/app.js"
  },
  "browserify": {
    "transform": [
      [
        "babelify"
      ]
    ]
  }
}
